# Requirements for deploy_sim2real / sim2sim environment
# 
# Setup:
#   conda create -n sim2sim python=3.10 -y
#   conda activate sim2sim
#   pip install -r requirements.txt
#
# Purpose: Load trained TWIST2 policies and test in simulation before real robot deployment

# PyTorch CPU-only (smaller download, sufficient for inference)
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0

# Core
numpy>=1.24.0

# ONNX export and inference
onnx>=1.14.0
onnxruntime>=1.15.0
onnxscript>=0.1.0

# MuJoCo simulation
mujoco>=3.0.0
